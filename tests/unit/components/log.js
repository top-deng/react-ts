import { expect } from 'chai';
export default {
  install: describe => {
    describe('********🐴 🐴 🐴 开始准备单元测试 🐴 🐴 🐴******', () => {
      it('模块加载成功', () => {
       expect(1).to.equal(1);
     })
   })
  }
};
